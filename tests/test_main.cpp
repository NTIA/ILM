#include <gtest/gtest.h>
#include "ilm.h"  // Include your shared library header

// Example test case
TEST(ILMTest, ExampleTest) {
    // Call a function from the shared library and check the result
    // Replace with actual function calls from the ILM library

    int result = 42;  // Example of a result
    EXPECT_EQ(result, 42);  // Example assertion: check if result is 42
}

// Add more tests as needed
